# dashboard

React dashboard: live request log, detection findings, and per-request cost.

Not scaffolded yet -- build it with the project's frontend design direction (dense, technical, dark, severity-coded) rather than a generic admin template. See the kickoff flow.
