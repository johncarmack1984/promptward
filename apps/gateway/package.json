{
  "name": "@promptward/gateway",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "OpenAI/Anthropic-compatible proxy: scans each request via tripwire-core, validates structured output, meters cost.",
  "scripts": {
    "dev": "echo 'TODO(build): start the proxy (Hono/Express). See src/index.ts.' && exit 1"
  }
}
